generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model VoiceSession {
  id          String   @id @default(cuid())
  userId      String
  guildId     String
  channel     String
  joinTime    BigInt
  leaveTime   BigInt?
  duration    Int?     
  createdAt   DateTime @default(now())

  @@index([userId, guildId, channel])
}

model ActivitySession {
  id           String   @id @default(cuid())
  userId       String
  activityName String
  activityType String
  startTime    BigInt
  endTime      BigInt?
  duration     Int?
  createdAt    DateTime @default(now())

  @@index([userId, activityName])
}

model Total {
  id           String @id @default(cuid())
  userId       String
  guildId      String
  type         String 
  activity     String 
  totalSeconds Int    @default(0)

  @@unique([userId, guildId, type, activity])
}